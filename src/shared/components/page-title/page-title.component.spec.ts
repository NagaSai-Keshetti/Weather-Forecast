import { PageTitleComponent } from './page-title.component';

describe('PageTitleComponent', () => {
  let component: PageTitleComponent;

  beforeEach(() => {
    component = new PageTitleComponent();
  })

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
